import React, { Component } from 'react';
import Header from '../Header';
const Contact = (prop) => {
        return (
            <div>
                <Header />
   hello             
            </div>
        );
    }

export default Contact;